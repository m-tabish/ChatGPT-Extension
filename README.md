### Class name for user chats
  bg-token-message-surface

# extract user message 
```javascript
(()=>{ const userMsg  = document.getElementsByClassName("whitespace-pre-wrap")[0].innerText; console.log(userMsg)})()
```